{
  "name": "hotel-ui",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:guest": "npm run dev --workspace=@hotel-ui/guest",
    "dev:admin": "npm run dev --workspace=@hotel-ui/admin",
    "build": "npm run build --workspace=@hotel-ui/guest && npm run build --workspace=@hotel-ui/admin",
    "build:guest": "npm run build --workspace=@hotel-ui/guest",
    "build:admin": "npm run build --workspace=@hotel-ui/admin",
    "lint": "npm run lint --workspace=@hotel-ui/guest && npm run lint --workspace=@hotel-ui/admin"
  }
}
